import React from 'react';

export default function Home() {
  return (
    <main style={{padding:40,fontFamily:'Arial'}}>
      <h1>Task Manager - Frontend (placeholder)</h1>
      <p>This is a starter placeholder for the Next.js frontend. I will implement signup/login and tasks UI next.</p>
      <ul>
        <li>API backend: <code>http://localhost:4000</code></li>
        <li>To get started: cd frontend && npm install && npm run dev</li>
      </ul>
    </main>
  );
}
